# Preview Server Feedback

Never use preview_start or preview_* tools. The user views the site directly on localhost and does not need a preview server started through Claude. Do not attempt to start, configure, or verify a preview server. Just run `npm run dev` in the background via Bash if a dev server is needed, and tell the user to open localhost themselves.
